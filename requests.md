# Requests — Experiment 3: Ark-wasm Language & Runtime

No outstanding requests. The experiment was completed successfully with all tests passing.

The ark-wasm language infrastructure is ready for use by downstream experiments:
- **Experiment 4** (Rewrite Rule Design & Validation) can import `ArkWasmLang` and define `egg::Rewrite` rules over it.
- **Experiment 5** (Equality Saturation Compiler) can build the full optimization pipeline using the language and evaluator.
